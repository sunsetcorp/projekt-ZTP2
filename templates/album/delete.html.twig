{% include 'default/_form.html.twig' with {
    page_title: 'title.album_delete'|trans({'%id%': album.id|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'album_index',
} %}
